
machine:
  node:
    version: 6.0.0

general:
  artifacts:
    - "test"

test:
  pre:
    - sh circle-deps.sh
    - npm i -g elm

  override:
    - ~/dependencies/sysconfcpus/bin/sysconfcpus -n 2 npm test

dependencies:
  cache_directories:
    - ~/dependencies
    - elm-stuff
    - tests/elm-stuff
