
machine:
  node:
    version: 6.0.0

general:
  artifacts:
    - "test"

test:
  override:
    - ~/dependencies/sysconfcpus/bin/sysconfcpus -n 2 npm test

dependencies:
  pre:
    - sh circle-deps.sh
    - npm i -g elm
